<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quizmaster">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".QuizTakerActivity"></activity>
        <activity android:name=".QuestionActivities.InputReaderActivity" />
        <activity android:name=".QuestionActivities.BulletpointActivity" />
        <activity android:name=".QuestionActivities.MathQuestionActivity" />
        <activity
            android:name=".StartingMenu.SharedQuizActivity"
            android:label="@string/title_activity_shared_quiz"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".StartingMenu.OldQuizActivity" />
        <activity android:name=".QuestionActivities.BasicQuestionActivity" />
        <activity
            android:name=".StartingMenu.QuizMakerActivity"
            android:label="@string/title_activity_quiz_maker"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>